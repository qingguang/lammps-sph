# dump forces on the swimmer

shell "mkdir -p" ${dname}/forces_swimmer/
dump               dump_forces_swimmer swimmergroup custom ${Nfreq} ${dname}/forces_swimmer/fs.*.dat id type x y z fx fy fz
dump_modify        dump_forces_swimmer first yes sort id pad 9
